{
  "name": "notion-wechat-ios",
  "version": "1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/background.ts src/content.ts --bundle --outdir=dist --target=chrome114 --format=esm --loader:.md=text",
    "dev": "esbuild src/background.ts src/content.ts --bundle --outdir=dist --target=chrome114 --format=esm --loader:.md=text --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.261",
    "esbuild": "^0.20.2",
    "typescript": "^5.4.5"
  },
  "dependencies": {
    "highlight.js": "11.11.1",
    "openai": "^6.27.0"
  }
}
